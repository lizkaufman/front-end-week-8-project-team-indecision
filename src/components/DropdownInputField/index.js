import React from 'react';

function DropdownInputField({ placeholder, name, value }) {
  return <input placeholder={placeholder} name={name} value={value}></input>;
}

export default DropdownInputField;
